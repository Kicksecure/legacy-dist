## Need to use systemctl directly so --no-pager --no-block can be used.
whonix-legacy: maintainer-script-calls-systemctl

## Not using dpkg database only maintainer scripts directly.
whonix-legacy: uses-dpkg-database-directly usr/lib/whonix-legacy/fixes
